---
title: "{ggplot2} Most Advanced Bar Plots in R"
description: |
  Get ready to uncover the secrets of producing visually stunning barplots masterpieces like never before, adorned with vibrant colors, captivating themes, and meticulously crafted labels. 
author:
  - name: Yury Zablotski
    url: https://yuzar-blog.netlify.app/
date: 2023-07-10
output:
  distill::distill_article:
    self_contained: false
draft: true
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```


# Basic barplots

## (bourne) Identity
## () Count

# Grouped barplots

# Stacked barplots

## Percent stacked barplots

# Multiple barplots

# Cercular barplots

# Fancy barplots

# Save your plots

```{r}
# install.packages("ggchicklet", repos = "https://cinc.rud.is")
```

# What's next? Barcharts with Significance Tests



# Rest

```{r}
library(ggplot2)

# Create some data
data <- data.frame(
  category = c("A", "B", "C", "D", "E"),
  count = c(10, 20, 30, 40, 50)
)

# Create the bar plot
ggplot(data, aes(x = category, y = count)) +
  geom_bar(stat = "identity", fill = "steelblue") +
  labs(title = "Bar Plot", x = "Category", y = "Count") +
  theme_classic() +
  theme(axis.text.x = element_text(size = 10, face = "bold"),
        axis.text.y = element_text(size = 10, face = "bold"),
        axis.title.x = element_text(size = 12, face = "bold"),
        axis.title.y = element_text(size = 12, face = "bold"))

geom_bar(stat = "identity", position = "dodge") +
  labs(x = "\n Cause Of Death", y = "Count \n", fill = "Age Group \n",
       title = "Suicide Method Data From The UK Results \n") +
  scale_x_discrete(labels = c("Drug", "Gas", "Gun", "Hang", "Jump", "Other")) +
  scale_fill_discrete(labels=c("Middle Aged", "Old", "Young")) +
  theme(plot.title = element_text(hjust = 0.5), 
        axis.title.x = element_text(face="bold", colour="blue", size = 12),
        axis.title.y = element_text(face="bold", colour="blue", size = 12),
        legend.title = element_text(face="bold", size = 10))
```

```{r}
# Load required packages
library(ggplot2)

# Create sample data
data <- data.frame(
  Category = c("A", "B", "C", "D"),
  Value = c(10, 15, 7, 12)
)

# Create the bar plot
plot <- ggplot(data, aes(x = Category, y = Value)) +
  geom_bar(stat = "identity", fill = "steelblue", width = 0.5) +
  labs(title = "Bar Plot", x = "Category", y = "Value") +
  theme_minimal()

# Customize plot features
plot <- plot +
  theme(
    axis.title.x = element_text(size = 12, face = "bold", color = "black"),
    axis.title.y = element_text(size = 12, face = "bold", color = "black"),
    axis.text.x = element_text(size = 10, color = "black"),
    axis.text.y = element_text(size = 10, color = "black"),
    plot.title = element_text(size = 14, face = "bold", color = "black"),
    panel.background = element_rect(fill = "white", color = "black"),
    panel.grid.major = element_line(color = "gray80", linetype = "dashed"),
    panel.grid.minor = element_line(color = "gray90", linetype = "dashed"),
    legend.title = element_blank(),
    legend.position = "none"
  )

# Adjust axis ticks
plot <- plot +
  scale_y_continuous(
    breaks = seq(0, max(data$Value), by = 2),
    limits = c(0, max(data$Value)),
    expand = c(0, 0),
    labels = function(x) format(x, scientific = FALSE)
  )

# Display the plot
print(plot)

```


